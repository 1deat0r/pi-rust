[package]
name = "pi-coding-agent"
description = "Pi agent harness — placeholder crate (see PLAN.md)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
