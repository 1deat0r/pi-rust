//! CLI command handlers — port of `packages/coding-agent/src/package-manager-cli.ts`
//! (package + config commands) and the auth command path in `main.ts`
//! (`cli/auth-command.ts`, `cli/auth-check.ts`, `cli/credential-print.ts`).

pub mod auth;
pub mod config;
pub mod package;
